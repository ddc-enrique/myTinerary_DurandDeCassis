const PreLoader = () => {
    return (
        <div className="preLoader">
            <img src={require("../assets/preLoader.gif").default} alt="Pre Loader" />
        </div>
    )
};

export default PreLoader;